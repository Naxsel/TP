/*
 * Autores: Ana Roig Jimenez, Alejandro Solanas Bonilla
 * NIAs:    686329, 647647
 * Fichero: Partida.java
 * Fecha: 26/02/2015
 */


import java.util.ArrayList;
import java.util.Random;

public class SieteYMedio extends Partida {

//Cada jugador recibe 3 cartas. Se suman los puntos de esas cartas. Gana el jugador que mas se acerca a 7'5 sin pasarse. Termina cuando no quedan cartas suficientes como para repartir a las 2 personas(no quean 6 cartas vaya).

	public Carta repartir () {
		Carta c = baraja.remove(r.nextInt(baraja.size()));
		return c;
	}
	
	public void suma(Carta[] c, int jugador) {
		double total = 0.0;
		for(int i = 0; i < 3; i++) {
			total = total + c[i];
		}
		System.out.printf("El total de puntos del jugador %d es %d", jugador, total);

	}


	main() {
		Carta c = new Carta[3];
		for(int i = 0; i < 3; i++) {
			c[i] = repartir();	
		}
	}

}
